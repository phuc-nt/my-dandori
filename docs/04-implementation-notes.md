# Implementation Notes — Dandori MVP

> Kiến trúc THẬT sau khi build (260702), quyết định chốt, giới hạn đã biết. Bổ sung cho [01-product-vision.md](01-product-vision.md) / [03-features.md](03-features.md) — không thay chúng.

## Kiến trúc

Một binary `dandori` (Go 1.26, pure-Go, no CGO), một file SQLite (WAL) tại `~/.dandori/dandori.db`.

```
cmd/dandori            entry
internal/config        defaults → YAML → env → .env (env thắng .env)
internal/store         SQLite + migrations (PRAGMA user_version) + testseed
internal/capture       hook ingest · transcript parse · attribution · watcher
internal/govern        guardrail engine · budget · gate · audit hash-chain · kill
internal/learn         4 metric · grade percentile · ROI · leaderboard · provenance · quality gate
internal/integrations  guard (DRY_RUN/AGENT_WRITE_DISABLED) · jira · slack · ghub
internal/web           chi + html/template + go:embed + HTMX console
internal/cli           cobra commands + serve workers
```

### CAPTURE — cách hoạt động thật

- `dandori init` merge 4 hooks vào `.claude/settings.json` (idempotent, giữ hooks sẵn có) + ghi `.dandori-agent` (agent/project attribution).
- `dandori hook <event>` đọc JSON stdin của Claude Code. **Fail-open**: lỗi nội bộ → stderr + exit 0, không bao giờ phá phiên người dùng.
- Token/cost: parse transcript JSONL (`~/.claude/projects/<cwd-encoded>/<session>.jsonl`), dòng `type=assistant` → `message.usage`, **dedup theo `message.id`, full-reparse + SET** (không cộng dồn incremental) → không thể đếm đôi. Reconcile throttle 10s/run ở post-tool, chốt ở Stop.
- Watcher (C7): quét transcript mtime mới, session chưa có run → tạo `source=watcher`; đã có → chỉ reconcile usage. Chạy trong serve (ticker 60s) + `dandori watch`.
- Cost = tokens × bảng giá trong config (`pricing`, USD/MTok, prefix-match model id, fallback `default`). Giá lệch thực tế → sửa config, không sửa code.

### GOVERN — thứ tự check (cố định)

`kill switch → sandbox (G2) → block rules (G1) → budget (G3) → gate (G4)` — hit đầu tiên thắng. **Fail-close** khi engine không eval được rule (ngược với capture). Mọi decision → event + audit entry (`hash = sha256(prev||ts||actor||action||subject||detail)`, `dandori audit verify` walk chain).

- Gate G4: tạo approval pending, poll DB 2s tối đa `gate_wait_seconds` (30s; hook timeout cài 40s). Approve trong lúc chờ → allow + consume; timeout/reject → deny kèm id. Web và Slack cùng quyết: **first-writer-wins** (UPDATE … WHERE status='pending').
- Sandbox G2 chỉ enforce cứng cho Write/Edit/NotebookEdit; path token trong Bash là heuristic (miss thì lọt — block rules đỡ các case nguy hiểm). Giới hạn ghi nhận, chấp nhận MVP.

### LEARN — proxy và giới hạn

| Metric | Công thức | Giới hạn proxy |
|---|---|---|
| Acceptance | 1 − rejected_edits/edits | chưa có revert-detection GitHub; reject signal chỉ từ tool_result ok=0 + guardrail block |
| Success | done/ended; có task_key → status Jira | run tương tác không link Jira dễ 100% |
| Autonomy | 1 − intervened_runs/runs (ask hoặc >1 user msg) | phiên interactive của người luôn bị tính intervened — đúng định nghĩa "supervised" nhưng làm autonomy=0 hàng loạt |
| Reliability | 1 − mean(err, block, kill rates) | — |

Grade: percentile composite trên fleet, A≥p80…F<p20; fleet <5 → static band + cờ `uncalibrated` (dấu `*`). ROI: 3 bucket loại trừ lẫn nhau (failed/killed → flagged → phần acceptance của clean) — không đếm đôi. Mọi metric trả `Formula` + run/event ids → `/provenance` drill về raw.

### Integrations

- **Cổng ghi duy nhất** `integrations.Guard`: `AGENT_WRITE_DISABLED` > `DRY_RUN` (mặc định true) > live. Skip → event `write_skipped`.
- Jira REST v3, basic auth; `/search/jql` fallback `/search`; site name nhận cả `phucnt0` lẫn `phucnt0.atlassian.net`. Flag→ticket: issuetype Task, label `dandori`, ADF description, key ghi ngược vào flag.
- Slack browser-token (xoxc bearer + cookie d=xoxd giữ nguyên URL-encode). Duyệt qua **reaction** (✅/❌, poll `reactions.get` 5s) vì browser-token không làm được interactive button. Reaction đầu tiên thắng, actor resolve qua `users.info`, ghi audit "via slack reaction". **Chưa có whitelist người react** — single-user MVP, RBAC [Sau].
- Alerts: events `budget_warn|guardrail_block|kill|flag` → post channel, dedup 2 tầng (per-event + per-topic-per-day) qua bảng `notifications`.
- GitHub: `gh pr list --json` → work_items (best effort). GWS: chưa dùng ở MVP.

## Quyết định chốt (từ câu hỏi mở của 03-features.md)

- Multi-runtime: chỉ Claude Code. G7: chạy lệnh check có sẵn (`gate_checks` config, chỉ đọc từ config local — không nhận từ web UI, tránh RCE). Kill switch: làm ở MVP. Điều khiển runtime: THẬT qua PreToolUse deny. RBAC: chưa, bind 127.0.0.1, actor = `user_name` config.

## E2E đã chạy thật (260702)

Chi tiết: `plans/reports/e2e-live-260702-dandori-mvp-report.md`. Tóm tắt: local 11/11 pass; live: Jira sync 21 issues SCRUM, approval Slack round-trip qua reaction thật (approved by phucnt0), flag → **SCRUM-22** tạo thật rồi transition Done, alert post + dedup, audit verify OK, self-capture qua watcher (chính phiên build này thành run có cost).

## Kết quả review (260702)

Code review độc lập: verdict **approve-with-fixes** — 0 CRITICAL. Đã fix ngay: **H1** audit hash-chain race giữa process (DSN `_txlock=immediate` + test 2-connection concurrent); **H2** Host/Origin guard trên console (chặn DNS-rebinding + cross-origin POST vào approval/kill switch — console không auth, bind localhost, guard này là trust boundary); **M1** lỗi ghi audit từ handler giờ log to; **M3** checkKill fail-close khi không đọc được state. Còn mở (cần quyết định sản phẩm): gate approval retry tạo pending mới & không tự expire; Slack reaction chưa whitelist người duyệt; leaderboard N+1 (chấp nhận quy mô nội bộ). Test/race/E2E xanh lại sau fix. Report: `plans/reports/from-code-reviewer-to-controller-260702-dandori-mvp-review-report.md`.

## v2 (260702, plan `260702-1117`) — surpass legacy

Đóng mọi gap mà 2 bản cũ còn thắng:

- **Approval lifecycle:** gate retry tái dùng pending (không spam queue); TTL 60' → `expired` (worker + lazy); Slack approver **whitelist** (`approvers` config / `SLACK_APPROVERS` env; rỗng = ai cũng duyệt, có cảnh báo).
- **Autonomy đo lại:** chỉ tính can thiệp GIỮA phiên (user msg sau lượt assistant đầu); prompt mở đầu không tính.
- **Compliance export** (`dandori export compliance`, `/export/compliance`, JSON/CSV): bundle audit chain + verify + approvals + flags + runs; bản thân export cũng ghi audit.
- **Multi-runtime:** `dandori wrap -- <cmd>` (fork/exec, passthrough IO, exit code giữ nguyên, git delta, adapter usage claude/codex/generic — không bịa số). Kill switch chặn cả wrap. Hooks vẫn là đường chính cho Claude Code (có guardrail per-tool-call; wrap thì không — giới hạn ghi rõ).
- **Git delta per run:** SessionStart snapshot HEAD+dirty → Stop tính `lines_added/deleted` + `head_before/after` → nền cho attribution & revert detection.
- **GitHub intelligence:** sync PR đầy đủ (created/merged/body) + map revert (`Reverts #N`); **AI-CFR** = reverted/merged (revert PR loại khỏi mẫu số); **PR cycle p50/p75**; `dandori sync reverts` quét `git revert` local map về run → event `revert_detected` → **acceptance giờ gồm tín hiệu revert thật** (formula nêu rõ 2 nguồn).
- **Attribution:** `dandori attribution` + số liệu ±lines/%reverted per agent.
- **Confluence 2 chiều:** `context show` (đọc page → text), `report confluence` + nút console (storage HTML: leaderboard/ROI/CFR/flags; dedup ngày; DRY_RUN guard). E2E live: page **3375105** tạo thật trong space MPM.
- **Analytics console:** spike detection (>3× median 7d, dedup/ngày, đẩy Slack) + trang `/spikes` explain; **DORA-lite** panel (lead time Jira, CFR, PR cycle; deploy freq = "n/a" trung thực); **run compare** `/runs/compare?ids=`; pagination runs.
- **Perf:** leaderboard 1-pass (hết N+1). Benchmark (M4 Max): `EvaluateAllow` **82µs/op** (mục tiêu <5ms), `Leaderboard` 50 agents × 1.000 runs **119ms** (mục tiêu <300ms).
- **Test depth:** coverage per package: govern 84 · integrations 100 · ghub 87.5 · learn 79 · jira/slack/confluence 77–79 · config 76 · store 71 · web ~70 · cli 61 · capture 63. Race clean.

### Review v2 (DONE_WITH_CONCERNS → đã fix)

2 HIGH + 7 MEDIUM đã xử lý trong cùng phiên: whitelist Slack chỉ match **user ID** (display name giả được) + quét MỌI reaction thay vì cái đầu tiên (chống griefing); approval **consume-once** cưỡng chế (N waiter chung 1 approval → chỉ 1 được allow); wrap fix misattribution FK + ignore SIGINT để luôn finalize run; compliance export **redact secret** (Bearer/xox/ATATT/sk-/ghp_) + CSV chống formula-injection + route đổi POST (side-effect audit); CFR chỉ tính revert PR đã MERGE + bỏ ref cross-repo; revert scan lọc theo cwd + prefetch dedup + guard hash regex; percentile nearest-rank; compare cap 5 ids; MidRunMsgs bỏ dòng sidechain/meta của subagent; dọn settings key khi run kết thúc.

## v3 (260702, plan `260702-1542`) — close the vision loop

Khép các lời hứa vision chưa giữ, trọng tâm LEARN & GOVERN (theo `plans/reports/dandori-v3-roadmap-assessment-learn-govern-260702-report.md`):

- **Autonomy bands (UA4+L6):** `supervised | gated | trusted` per agent — grade CÓ hệ quả. supervised: mọi edit cần duyệt; trusted: bỏ gate thường, rule `critical` vẫn gate; block/sandbox/budget/kill KHÔNG bao giờ nới. UI dropdown + `dandori band`. Đổi band luôn audit.
- **Closed loop G8 (gap #1):** worker 6h + `dandori loop run`: grade **F** (≥3 runs) → flag + Jira ticket (label `dandori-governance`, DRY_RUN guard) + **auto-demote** supervised; grade **D** → đề xuất demote vào review queue (người duyệt → applier áp dụng, consume-once); hồi phục A/B/C → flag tự resolve. Toàn chuỗi audit. Fix kèm: **GradeFor tie-handling mid-rank** (fleet toàn điểm bằng nhau không còn rơi cả cụm xuống F).
- **AI review tiếng người (hoàn thiện L1):** OpenRouter (`OPENROUTER_API_KEY/MODEL`), input chỉ là metrics+formula (không raw code), cache theo tuần, nhãn "AI-generated — verify at provenance". Lazy-load trên agent page + `dandori review`. Live-verified.
- **Human baseline (hoàn thiện L4 "kể cả của người"):** composite proxy = done-ratio per Jira assignee (≥3 items), **ẩn danh** — chỉ đưa vào phân bố percentile, UI ghi "X agents + Y anonymous human baselines". Tắt bằng `calibrate_with_humans: false`.
- **Grade confidence:** <5 runs → cờ low-confidence (mờ + "?"), closed loop tự bỏ qua mẫu nhỏ.
- **Policy simulator UE4 + rule builder UE2:** form tạo rule (kind/scope/critical) + **Simulate trên N ngày events thật** trước khi bật (hits + drill về run); rule theo scope global/agent/project; CLI `dandori rules simulate`. Create/delete rule đều audit.
- **Playbooks UG4 (flywheel):** run done → "Save as playbook" (prompt đầu, top files, model, chuẩn cost) → trang `/playbooks` copy được. Tri thức ở lại.
- **Why-failed UF5:** run failed/killed có banner verdict rule-based (loop lỗi lặp / block cuối / lỗi cuối) + tô đậm evidence rows trong timeline.
- **Hardening:** secret **redact tại INGEST** (package `internal/redact` dùng chung — DB không còn giữ token thô; export redact lần 2); escalation SLA (approval pending quá 4×gate-wait → event → Slack, trước khi TTL expire); budget **projected EOM** theo burn-rate.

## v4 (260702, plan `260702-2040`) — Executive: sản phẩm cho CEO nhiều team

Nâng Dandori từ harness một-người thành sản phẩm điều hành cho CEO công ty nhiều đội. Toàn bộ đã qua red-team plan review (4 CRITICAL + 5 HIGH vá trước khi code).

- **Central mode (P1):** hook máy dev parse transcript **tại chỗ** và POST **chỉ số liệu đã redact** (`internal/ingest/derive.go`) — server KHÔNG bao giờ mở transcript của máy khác. **Hai listener một binary:** console `127.0.0.1:4777` (originGuard, không auth — không bao giờ public) + ingest listener riêng `0.0.0.0:4778` (Bearer token, `crypto/subtle`, không có route console). Idempotency: mỗi event 1 ULID + UNIQUE index → spool replay không đếm trùng. Pre-tool GOVERN chạy **local** trên máy dev qua policy snapshot cache (không round-trip/call → không nghẽn writer đơn); mất cache + server chết → chỉ chặn lệnh ghi (fail-closed hẹp). Read-only pool tách khỏi writer. `dandori connect` (token vào file 0600), `dandori relay`, `dandori team`.
- **Operators + Teams (P1):** `operators` (người điều khiển, id = `username@hostname` lấy từ token principal), `teams`/`team_members` (gồm operator + agent). Team là khái niệm hạng nhất để so sánh hiệu quả giữa đội.
- **Behavior analytics agent+human (P2):** đo cả agent lẫn người: prompt proxy (số từ + cờ specificity — tính **client-side**, không lưu prompt thô), steering (`MidRunMsgs`), retry loop, permission-ask, abandonment, task-size. Rollup theo operator/team, `TeamCompare` so đội-với-đội, 1-pass. **Chỉ số của người là RIÊNG TƯ, không publish** (steering nhiều khi là ĐÚNG — chống Goodhart).
- **Master Observer (P3):** worker 30 phút + `dandori observe run` sinh insight có kiểu: `budget_overshoot_trend` (→ approval CEO), `agent_underused`/`operator_over_steering`/`playbook_candidate` (→ auto surface, INTERNAL-only, không ghi ngoài). Đề xuất rule kỹ thuật đi về surface **operator**, không vào inbox một-chạm của CEO. Không bypass: action nhạy cảm chỉ tạo `approvals`; applier riêng áp dụng sau khi người duyệt, consume-once + bù trừ. `expireStale` miễn trừ cả `observer:%`.
- **Flywheel (P4):** `DetectCandidates` (run sạch + prompt rõ + ít can thiệp) → `PromoteCandidate` thành playbook card → `flywheel publish` lên Slack + Confluence (DRY_RUN guard, dedup/ngày). Card mô tả **mẫu/agent**, KHÔNG có tên/xếp hạng người. Adoption tracking: ghi ai dùng playbook + đo done-rate trước/sau (riêng tư).
- **CEO Chatbot (P5):** `internal/chat` — OpenRouter tool-calling non-stream (`OPENROUTER_MODEL`), loop nhiều lượt, `parallel_tool_calls=false`. Read tools gọi hàm nội bộ (stats/leaderboard/team/run/insights). Action tools (`request_kill/budget/band`) **chỉ TẠO yêu cầu duyệt** — KHÔNG có tool nào decide/consume approval → chatbot không thể là đường lách. Guard: câu hỏi số liệu không có tool result → "chưa lấy được số liệu" (không bịa); model không hỗ trợ tool → tắt chat có thông báo VI; turn cap + ngân sách token/ngày; mọi tool call audit + redact. UI HTMX `hx-post` một fragment (không SSE).
- **Executive UI hai chế độ (P6):** trang chủ VI cho CEO — headline "Giá trị AI mang lại" + trend, thẻ đội đèn giao thông (ngưỡng named-const green≥0.66/amber≥0.40/đỏ/xám), inbox Duyệt/Bỏ qua (qua đúng đường decide đã audit), nhận định plain-VI, link trợ lý. **Mode switch** (Điều hành ⇄ Kỹ thuật, cookie) giữ nguyên 13 trang operator cũ. Copy duyệt trung tính "Đã duyệt (bảng điều khiển)" (single-principal, không nhận vơ per-human).
- **UAT tiếng Việt (P7):** `docs/uat-test-cases.md` — kịch bản bấm-tay cho người low-tech kiểm thử toàn console.

## v5 (260703, plan `260703-2101`) — Context Hub

Khép mảng vision còn trắng: **C5** (context phân tầng), **C6** (version/diff/rollback), **UD1–UD4** (editor/history/promote/preview) + **cầu nối thực thi** — context thật sự được tiêm vào phiên Claude Code. Qua red-team (1 CRITICAL + 3 HIGH vá trước khi code).

- **Context phân tầng (C5):** `contexts` (layer company/team/agent, target `*`/team-id/agent-id) + `context_versions` (immutable full snapshot) + `context_heads` (con trỏ head). Migration 011 additive.
- **Effective context = concatenation** company→team→agent (KHÔNG override): chính sách công ty **luôn áp dụng**, đội bổ sung, agent tinh chỉnh. Header cố định + boundary marker; budget theo **rune** (VN đa-byte) 3800/3800/2000, total-cap ≤10k, luôn giữ marker đóng (H2). Literal `dandori-context:` trong nội dung bị **neutralize** để không giả mạo được marker (M2). Whitespace-only head = coi như retire.
- **Version/diff/rollback (C6, UD2):** mỗi lần sửa = version mới bất biến; diff line-level bằng `github.com/hexops/gotextdiff` (**dep mới đầu tiên sau lâu — pure-Go no-CGO**, cross-compile verified); rollback = version mới copy nội dung cũ (lịch sử không xoá). version_n atomic theo MAX + 1 retry khi đua UNIQUE.
- **Company layer approval-gated (C1):** sửa/khôi phục chính sách **công ty** không ghi thẳng — tạo `RequestAction("context-company-edit", surface=operator)` → applier ghi sau khi duyệt (consume-once). Team/agent sửa trực tiếp có audit. Đây là mutation nhạy cảm nhất sản phẩm (tiêm vào MỌI agent) nên không có đường bypass.
- **Promote team→company (UD3):** đề xuất ghim `{source_version_n}` — applier áp đúng bản người duyệt đã nhìn, KHÔNG áp head hiện tại (H3 TOCTOU); nếu đội đã sửa sau đề xuất thì audit ghi chú. Dedup 1 đề xuất/đội.
- **Secret fail-loud (M1):** `SaveContext` **từ chối** nội dung chứa chuỗi giống secret (không redact im lặng — sẽ làm hỏng nghĩa policy toàn org). Báo lỗi VI nêu đoạn vi phạm. Cái giá: prose hợp lệ như "Bearer token" cũng bị chặn, phải viết lại.
- **Cầu nối tiêm (điểm ăn tiền):** hook `SessionStart` trả `hookSpecificOutput.additionalContext` = effective context (chỉ source startup|resume; **fail-open** tuyệt đối — lỗi/DB hỏng → exit 0, phiên vẫn chạy). Local đọc DB; central `GET /ingest/context` (TTL cache, 500-on-error để không đè stale cache tốt — M4). CLI `dandori context show --effective <agent>`.
- **Provenance:** run ghi event `context_injected` payload `{company:v3,team:v5}` — biết run chạy với context version nào. Central là eventual (FlushThrottled).
- **UI hai mode:** editor + history/diff/rollback + preview trong khu **Kỹ thuật** (📚 Context dưới nhóm Learn); exec_home có card đọc-chỉ "Tri thức công ty: N chính sách · cập nhật gần nhất".
- **E2E hành vi thật (proof quyết định):** seed nonce vào company context → chạy phiên `claude -p` thật → hỏi "mã kiểm chứng?" → model trả **đúng nonce**. Chứng minh model NHẬN và DÙNG context tiêm vào (không chỉ emit JSON). `scripts/e2e_context_inject.sh` 6/6.

## v6 (260704, plan `260704-0614`) — Commander

Biến Dandori từ "quan sát/gác" thành "chỉ huy": phóng và điều khiển agent-run trực tiếp từ console (UA1/UA5/UA6). Đây là milestone **RCE-surface** nhất (web server spawn process) — qua red-team 3 CRITICAL + 5 HIGH vá **trước khi** code, E2E chứng minh flag-injection vô hại + kill giết process thật.

- **Phóng bất đồng bộ (UA1):** package `internal/runner` — `Launch(agent,prompt,cwd,by,retryOf)` tạo run `source='console'`, `exec.Command` arg-slice (KHÔNG shell), `Setpgid` cho nhóm process riêng, `cmd.Start()` + goroutine reap; web handler trả ngay, HX-Redirect sang trang chi tiết live. Chia sẻ core capture với `dandori wrap` qua `internal/capture/runcore.go` (StartRun/FinalizeRun — DRY).
- **Chống RCE (điểm sống còn):**
  - **Không shell.** Prompt người nhập là **giá trị của `-p`** (`argvFor` → `[bin,"-p",prompt]`), không bao giờ đứng vị trí flag → prompt `--dangerously-bypass-approvals-and-sandbox` chạy như text (E2E assert).
  - **Chỉ launch claude** (đi qua hooks → guardrail + Context Hub injection; governed). Codex/khác = [Sau] vì wrap không gác được tool-call.
  - **Binary allowlist** đường tuyệt đối (`agent_binaries` trong config, `os.Stat`+`IsRegular`, không bao giờ `$PATH`). **cwd allowlist** trong `projects_dir` (`filepath.Rel` boundary — chặn cả `../` lẫn sibling `/proj-evil`).
  - Launch/kill/retry/bulk **POST sau originGuard**, chỉ console localhost, KHÔNG lên listener ingest. Kill-switch đọc **live** trước spawn. Mọi hành động audit.
- **Lifecycle (UA lifecycle):** kill thật = `syscall.Kill(-pgid, SIGTERM→5s→SIGKILL)` **trong cùng lock** với reaper (không hit PID tái dùng — C3) + `govern.KillRun` (status+audit). `killed` là terminal truth (reaper không hạ xuống `failed`). Semaphore giới hạn concurrent, release mọi nhánh (kể cả panic). Reap: drain pipe (`wg.Wait`) **rồi** `cmd.Wait` (chống deadlock StdoutPipe). Startup **reconcile**: run `console` PID chết → `failed`; PID sống → **nhận nuôi** (re-register pgid) để vẫn kill được; hook/wrap run không bị đụng.
- **Live UI (P3):** trang chi tiết run console poll HTMX `every 2s` — badge trạng thái + nút Dừng có điều kiện, log stream tăng dần theo `events.id` cursor, dừng poll bằng **HTTP 286** khi run kết thúc. Output agent HTML-escape (XSS). Orphan nhận-nuôi hiện "không có log trực tiếp nhưng vẫn dừng được".
- **Retry-with-fixes (UA5):** từ run console đã xong → form điền sẵn prompt (sửa được) → phóng run mới, `retry_of` **lấy từ URL path** (không phải form field — chống giả mạo lineage M5); hai chiều link trên trang chi tiết.
- **Bulk (UA6):** checkbox + select-all trên trang Runs → bulk-kill / bulk-set-budget, mỗi item qua đúng đường audit đơn lẻ. Tally **trung thực**: "N đã dừng process · M chỉ đánh dấu · K lỗi" — không bao giờ nói dối "all killed" (M4).
- **Migration 012:** `pid, launched_by, exit_code, retry_of` (additive). Dep mới: `golang.org/x/sync/semaphore`. Nav: 🚀 Phóng run (nhóm Capture, khu Kỹ thuật).
- **E2E hành vi thật:** `scripts/e2e_commander.sh` 8/8 — fake claude echo argv chứng minh prompt-nguy-hiểm thành `-p` value; launch→done sạch; kill process thật chết (`kill -0` fail), status=killed, audited; cross-compile linux no-CGO.

## v7 (260704, plan `260704-1058`) — Write-back & vision closeout

Đóng nốt các ô GOVERN/LEARN còn `[Sau]`: Dandori giờ **ghi** vào Jira/GitHub/Calendar/Sheets/Gmail/Drive, không chỉ đọc — khép UC2/UC4/UC6/UC8/UC9/UB4/UE3/G6/L7/UG2b/UG3/UG5. Milestone **write-surface** nhất sau Commander (v6): mọi write ra service ngoài đều qua đúng một khuôn — `RequestAction` (đề xuất, không bao giờ tự thực thi) → người duyệt ở `/reviews` (inbox có sẵn từ v4, không dựng UI riêng) → `RunObserverApplier` (consume-once) → client thật (`jira`/`ghub`/`gws`) sau `integrations.Guard`.

- **Approval-gated write actions (UC2/UC4/UC9):** 3 case mới trong `applyInsightAction` — `jira-transition`, `pr-review`, `calendar-event`. Request handler chỉ **pin evidence tại thời điểm request** (transition id/name, PR head SHA, thời gian RFC3339 đã convert từ input `datetime-local` không offset) — không bao giờ suy luận lại tại apply time. **H3 (re-openable, không mất âm thầm):** nếu target đã đổi giữa lúc duyệt và lúc áp dụng (tên transition không còn tồn tại, PR đã merge/đổi head) → audit `*_apply_stale` + insight advisory mới, KHÔNG âm thầm bỏ qua và KHÔNG áp nhầm. Calendar dùng UNIQUE trên `notifications.dedup` (claim-before-write) để retry cùng `idem_key` chỉ tạo một event thật.
- **Delivery config-pinned (UC8/UG2b):** `SheetsExporter.Export` và `DigestPublisher.Send` đọc đích **chỉ từ config** (`export_spreadsheet_id`, `digest_recipients`) — không route/handler nào có field nhận đích từ request. Sheet chưa pin → tạo 1 lần rồi lưu id vào `settings` để tái dùng (không tạo sheet mới mỗi lần). Dedup key gồm **hash tập người nhận** (`M4`) nên đổi recipient trong cùng ngày không bị coi là trùng.
- **Drive import mọi layer approval-gated (UC6):** `DriveImporter.Review` trả **full body** (không cắt) cho reviewer thấy đúng cái sẽ ghi; import ở CẢ BA layer (team/agent/company) đều tạo `RequestAction("context-import")` — không có layer nào ghi thẳng, kể cả agent/team vốn trước đây ghi trực tiếp ở v5. `/reviews` hiển thị full body (không phải preview 2000 ký tự) vì đây là ranh giới tin cậy thật.
- **Gate override per-check (UB4):** `OverrideGate` sửa đúng 1 dòng `gate_results` (`UPDATE ... WHERE overridden_at IS NULL`) — override lần 2 cùng check là no-op (0 dòng, không audit trùng), reason trống → 400. Flag đánh dấu resolved khi check cuối cùng được override, không xoá bản ghi.
- **Post-action check (G6):** `learn.PostCheck` chạy sau khi run finalize, cwd = working dir run đó. **Mặc định danh sách rỗng** (opt-in tuyệt đối) — không cấu hình gì thì không exec gì. Check fail chỉ log + flag, không chặn finalize (non-blocking).
- **Gate threshold UI (UE3):** `gate_min_grade`/`gate_min_pass_pct` chuyển từ hằng số sang `settings` — form `/gate-thresholds` ghi, `MeetsMinGrade`/`DetectPlaybookCandidates` đọc cùng key nên đổi ngưỡng có hệ quả ngay tại điểm quyết định, không phải chỉ đổi hiển thị.
- **Agent assignment (L7):** `SuggestAgents` xếp hạng theo lịch sử done-rate + keyword overlap giữa task mới và task cũ đã giao — **suggest-only**, không có route POST nào tự gán; PO luôn là người click assign thật (route hiện có).
- **Saved views + wallboard (UG3/UG5):** `saved_views` (migration 014) — tên qua `html/template` auto-escape (L2, chống XSS trong dropdown) + length cap. Wallboard là fragment poll tái dùng data đã có (running count, spend vs budget, queue depth) — không thêm bảng mới.

## v8 (260705, plan `260705-1512`) — Onboarding & Executive UX

Đóng lớp giao tiếp người dùng cuối: backend đã đủ mạnh (05: GOVERN ~90%), cái thiếu để một tổ chức low-tech + CEO không đọc code adopt được là onboarding + tầng executive. Milestone **zero-migration** (mọi cache/dedup qua `settings` + `notifications` sẵn có). Qua red-team 4 lens (18 findings applied) trước khi code; 3 findings kiến trúc lớn đổi hẳn hình dạng vài phase.

- **Credential UI + healthz (Phase 1-2):** `/settings/integrations` dán token + Test (probe read-only per integration) → ghi `./.env` write-through. **`envwrite.go` là contract bảo mật:** closed keyset map (`{jira,slack,openrouter}→exact keys`, KHÔNG prefix) chặn tuyệt đối `DRY_RUN`/`AGENT_WRITE_DISABLED`/`SLACK_APPROVERS`; value sanitize chặn newline-injection (một `\n` trong value từng có thể chèn dòng `.env` thứ hai tắt guardrail); atomic temp+rename với `O_EXCL 0600` + `fsync` trước rename + package mutex (no lost-update). `originGuard` reject `Origin: null` cross-site trên POST (iframe-sandbox CSRF). Probe validate `ATLASSIAN_SITE_NAME` là DNS-label trước mọi network call (SSRF). Secret không bao giờ echo (mask 4 ký tự cuối) / log / vào audit detail (chỉ tên integration). `/healthz` từ `{"ok":true}` thành `Collect()` (package web, không package riêng — 0 consumer ngoài web): db + integration status + `hooked_projects` + `age_days` cho last-test.
- **Restart-required (chốt bởi red-team):** save credential ghi `.env` + `os.Setenv` (cho probe ngay) NHƯNG cần restart để worker nhận — workers bind config lúc boot (`serve.go`), `slackWorker` thoát vĩnh viễn nếu Slack chưa config lúc boot. KHÔNG hot-swap config (`s.Cfg` đọc ở 56 chỗ + workers boot-bound). Save flow báo rõ.
- **Onboarding wizard (Phase 3):** `/welcome` 3 bước derive từ health (không state mới): hook project ⇔ `hooked_projects>0` · nối integration ⇔ configured+test-ok · run đầu ⇔ `runs_captured>0`. Để bước 1 ✓ ngay sau `dandori init` (không chờ có run), init ghi thêm settings key `hooked:<project>` vào DB trung tâm. Banner first-run trên exec home tới khi có run. Poll 5s, 286 stop khi cả 3 ✓.
- **Proactive notifications (Phase 4, viết lại bởi red-team):** KHÔNG xây `notify.go` mới — `slack.Alerter` đã quét events + post Slack cho `budget_warn`/`closed_loop`/`flag` mỗi tick, dedup qua `notifications`. v8 chỉ **hoàn thiện**: thêm event `flag_stale` (scan trong closed-loop tick, dedup, best-effort không abort cycle) + `formatAlert` tiếng Việt nghiệp-vụ + deep link (`BaseURL` từ config, không từ request). Govern EMIT event (rẻ), Alerter POST async serve-side — KHÔNG post trong hot-path guardrail (`emitBudgetWarn` chạy trong hook process, post Slack đồng bộ ở đó = latency guardrail).
- **Risk overview (Phase 5):** `/risk` reuse partial `reviews_pending` + `budgets_table` (DRY, không nhân bản data đã ở standup) — chỉ khối "agent D/F + tuổi + trend" là code mới. Poll 10s, empty-state ✓ (trạng thái tốt phải nhìn thấy).
- **Impact estimate (Phase 6, advisory-minimal):** approval card thêm dòng "ước lượng từ N lần trước: trung bình $X, đụng Y file". `EstimateImpact` runs-first query (index `idx_runs_agent`, không LIKE full-scan), exclude synthetic action (`band-demote:`/`observer:` — cost run cho đề xuất demote là số vô nghĩa), ngưỡng ≥3 mẫu, cache qua `settings` (không map+mutex). **Two-pass query** (drain cursor + Close trước khi QueryRow) tránh deadlock single-connection (`SetMaxOpenConns(1)`) — bug này đã bị test bắt và fix.

### Bảo mật (bắt buộc đọc trước khi bật)

- **H2 — Post-action check là RCE theo thiết kế:** `sh -c "<check>"` chạy trên cwd **đã bị agent sửa** — một `_test.go` độc hoặc `go generate` cài sẵn bởi chính agent có thể tự thực thi dưới quyền user chạy Dandori, tự động, sau MỌI run. Mitigant duy nhất: `post_action_checks` **mặc định rỗng**, chuỗi check chỉ đến từ `config.yaml` do operator sở hữu — KHÔNG BAO GIỜ từ DB, web UI, agent, hay task text. Khuyến nghị: ưu tiên check không-thực-thi-code-agent như `go vet ./...`; tránh `go test ./...` hoặc bất cứ gì gọi `go generate` trừ khi đã tin agent tuyệt đối.
- **C1 — Nội dung nhập từ Drive là input không tin cậy:** doc Drive được người ngoài tổ chức (hoặc chính agent trước đó) có thể chỉnh sửa, rồi bị tiêm thẳng vào context mọi agent qua `SessionStart` (`internal/contexthub/inject.go` → `additionalContext` → system-reminder). Vì đây là đường tiêm **instruction** thật (không phải data hiển thị), approval-gating áp dụng cho **cả ba layer** team/agent/company — không có layer nào "đủ thấp rủi ro" để bỏ qua người duyệt. Review UI luôn hiện **full body**, không cắt ngắn: một payload nguy hiểm nằm sau ký tự thứ 2000 sẽ không ẩn được ở đúng điểm quyết định.
- **C2 — Đích gửi (digest/export) là config-pinned, không phải tham số:** không có field `to`/`spreadsheet_id`/`recipients` nào trong bất kỳ form hay route ghi nào của UC8/UG2b — destination chỉ tồn tại trong `Cfg.DigestRecipients`/`Cfg.ExportSpreadsheetID` do operator set qua config file. Một request giả mạo mang theo trường "đích" chỉ bị bỏ qua, không có code path nào đọc nó.

## v9 (260706, plan `260706-2030`) — Capture-gap + dense-data insight

Red-team (verify bằng sqlite3 trên 36 run thật) đảo hình dạng milestone: bản đại tu công thức grade ban đầu vẽ cho data **chưa tồn tại** (`task_key` 0/36, `lines_added` 0/36, duration dương 3/36, `user_msg` chỉ đếm, guardrail 0/0/0). Thứ tự đúng: **vá capture trước, công thức sau**. v9 = Milestone A (capture-gap) + B (insight trên data đã dense). Đại tu công thức (Milestone C) tách sang plan riêng, chỉ mở khi density đo lại đủ.

- **Watcher end-time (Phase 1):** thay `ended_at = file mtime` (nguồn 31/36 duration âm) bằng timestamp event thật trong transcript — `ParseTranscript` đọc field `timestamp` mỗi dòng, `reconcileWatcherTimes` SET `started_at`/`ended_at` cho watcher-run với data-quality gate (`LastTS > FirstTS`, thiếu → NULL không bịa). Hook/wrap path giữ nguyên (Stop là chuẩn). `watch --full` re-scan backfill. **Kết quả: duration dương 3→30, âm 31→0.**
- **task_key linkage (Phase 2):** chuỗi ưu tiên **branch → commit trailer → transcript scan** (`FindTaskKeyChain`), mỗi candidate qua `validTaskKey` (exact/prefix-match `work_items` — key không tracked bị **bỏ**, không đoán). Provenance event `task_key_source`. **0→1 link validated, 0 mislink** (đa số run dev trên `main`, không có Jira key — honest coverage thấp).
- **git-delta watcher (Phase 3):** `snapshotStart` (dùng chung hook + watcher) + `recordGitDelta` tại stale-close cho watcher path. **Audit hook-0-lines: root cause = hook CHƯA CÀI** (`head_before` NULL 7/7) — setup gap, không phải bug (path đúng, test `TestHookPathRecordsLines` chứng minh). Delta lịch sử không backfill được (before-state mất); run watcher mới capture đúng (3 run đã có delta thật).
- **steering capture (Phase 4):** `user_msg` từ chỉ-đếm → thêm 1 event `steering_msg`/message với text (redacted qua `redact.String`, truncate 4096, delete+insert idempotent). **0→29 run có steering, 540 event.** KHÔNG classify corrective-vs-not (để v10 — red-team: content-heuristic thưởng giấu-feedback). Verify 0 secret-value leak (chỉ prose nhắc tên token).
- **guardrail capture audit (Phase 5):** producer đã tồn tại (`engine.record()` ghi `guardrail_block`/`permission_ask`; `KillRun` ghi `kill`). Unit test production-path + **live E2E** (`hook pre-tool` với `rm -rf /` → deny + event + audit). **Kết luận: 0/0/0 là honest-zero** (fleet chưa vi phạm + hook chưa cài), không phải capture gap. Findings: `plans/260706-2030-.../reports/phase-05-guardrail-capture-audit-findings.md`.
- **Insights page (Phase 6-7, Milestone B):** `/insights` — hiệu suất theo model (cost, done-rate, cost/done, cache-hit), tận dụng cache theo phiên, cost-per-outcome theo project/agent. Descriptive stats thuần (không grade/weight/calibration). **Bất biến "insufficient data ≠ so-sánh-được":** ngưỡng `MinSampleForInsight=3` in rõ trên UI (không cliff ẩn), model/group n<3 gắn badge "chưa đủ mẫu", cost/done hiện tử/mẫu (`$X / N done`), running loại khỏi mẫu số. Server-render + HTMX group-toggle, không SPA.

**Thứ tự sau v9 (chốt 260707): v10 = Identity & RBAC, metric overhaul lùi thành v11.** Lý do: metric overhaul bị gate bởi data-density (phải chờ run MỚI đã cài hook tích luỹ 1–2 tuần), còn identity — blocker Tier-1 của `docs/05` — không cần chờ data, làm được ngay trong lúc chờ. Điều kiện mở plan metric-overhaul (v11): density đạt — duration dương đa số ✓ (30/37), task_key có JOIN ✓ (dù thấp), steering text tồn tại ✓ (29 run), guardrail leg live-PASS ✓. **Nhưng** task_key coverage + git-delta lịch sử vẫn mỏng (data mất, không backfill được) → v11 phải chờ tích luỹ run MỚI (đã cài hook lên 18 project ngày 260706) trước khi reweight composite; reweight phải show before/after ranking delta thật, delta ~0 thì hoãn (YAGNI).

## v10 (260707, plan `260707-0000`) — Identity & RBAC

Đóng 3 Tier-1 blocker của `docs/05-product-gap-assessment.md`: console single-principal `@console`, central-mode 1 token chung + principal client tự khai, không RBAC. Qua red-team trước cook (C1–C5, H1, H3, H4 chốt trong `plan.md`), cộng 2 fix phát sinh lúc review P2-P4.

- **Local login (Phase 1):** `operators` table mở rộng (migration `015_identity_rbac.sql`, additive) thêm `username`/`password_hash`/`role`/`disabled_at`, cộng bảng `sessions` + `api_tokens` — không tạo bảng users song song. Hash argon2id qua `golang.org/x/crypto` (pure-Go, giữ no-CGO). Session store tay viết trong SQLite (`internal/auth/session.go`): id 32-byte, idle timeout 20 phút, absolute timeout 8 giờ, rotate session id lúc login (chống fixation). Rate-limit trong-tiến-trình (`internal/auth/ratelimit.go`): token-bucket per-IP (capacity 5, refill 1/30s) + per-account counter (khoá 5 phút sau 5 lần sai liên tiếp) — lỗi sai luôn trả thông điệp chung chung "Sai tên đăng nhập hoặc mật khẩu." (không lộ tồn tại username). Bootstrap account đầu qua CLI (`dandori operator add`), không qua web form (tránh race tạo admin đầu tiên qua request không auth).
- **Trust-gate C1/C2 (`internal/web/auth_middleware.go`):** local-trust (không cần login, như v9) chỉ còn đúng 1 điều kiện — **chưa từng có account nào được tạo** (`Store.HasEverHadAccount()==false`) **và** bind loopback (`127.0.0.1`/`::1`). Có account rồi (kể cả account đó bị disable sau) → mọi route non-public bắt buộc session hợp lệ, không session → redirect `/login`; request HTMX (`HX-Request`) nhận header `HX-Redirect` thay vì swap login HTML vào fragment. Bind non-loopback + 0 account → mọi path (trừ `/login`) trả trang bootstrap hướng dẫn `dandori operator add`, không bao giờ cấp admin.
  - **Fix C1/H4 (ngoài phạm vi phase gốc):** trust-gate ban đầu dự định key theo `HasAnyAccount()` (đếm account đang *enabled*) — bug: disable admin cuối cùng sẽ đưa count về 0, hệ thống lại rơi về local-trust và **mở khoá console cho mọi người không cần login**. Sửa: gate key theo `HasEverHadAccount()` (đã từng tạo, kể cả account đã bị disable) — disable admin cuối cùng giờ **khoá console** (redirect `/login`, không ai vào được kể cả loopback) thay vì mở lại no-auth. Test: `TestTrustGateDisablingLastAdminLocksConsole` (contrast với instance thật-sự-chưa-từng-có-account vẫn giữ local-trust v9).
- **Principal propagation (Phase 2):** mọi điểm dựng actor cho audit (`s.actor(r)` — kill/launch/retry/bulk/band/rules/chat/context/budget/gate-override/...) gộp về một helper đọc principal từ session context; fallback `Cfg.UserName+"@console"` **chỉ còn** trong nhánh local-trust (chưa từng có account). Sau P2: `grep -rn '@console\|Cfg.UserName' internal/web/*.go` (loại `_test.go` và `handlers_dash.go` — display-only) = 0 ngoài fallback local-trust. Namespace actor thực tế trong hệ thống: `<username>` (console, do operator đăng nhập) và `slack:<user_id>` (duyệt qua Slack reaction — namespace tách biệt console principal, xem `internal/integrations/slack/approvals.go`); audit hash-chain không quan tâm hình dạng actor string, `Verify()` pass xuyên ranh giới entry cũ (`phuc@console`) và mới (`alice`) — test `TestAuditChainMixedActorNamespaces`.
- **Per-operator ingest token (Phase 3, `internal/ingest/`):** central-mode dual-accept — Bearer token trước tiên thử khớp `api_tokens` (SHA-256 hash lookup, `LookupToken`) → attribute run về đúng operator sở hữu token; không khớp thì fallback token chung cũ (`Cfg.IngestToken`, cờ `AllowLegacyIngestToken` YAML `allow_legacy_ingest_token`, mặc định `true` — cho phép cutover dần). **[H1]** dù legacy hay per-operator, header client-supplied `X-Dandori-Principal-Hint` **không bao giờ** được tin để suy principal — token per-operator dùng operator đã bind sẵn ở DB; token legacy luôn attribute về hằng số cố định `legacyPrincipal = "legacy-shared@ingest"` bất kể header gửi gì (test `TestIngestPerOperatorTokenRejectsSpoofedHint`, `TestIngestLegacyTokenSpoofedHintStaysFixedPrincipal`). Cửa sổ dual-accept đề xuất 2 tuần (`plan.md` unresolved #4) — **ngày cutover cụ thể (tắt `AllowLegacyIngestToken`) chưa chốt**, dự kiến v11.
  - **Fix MEDIUM (ngoài phạm vi phase gốc):** ban đầu disable một operator chỉ chặn token khi bị revoke thủ công — token chưa revoke vẫn sống dù operator đã off-board. Sửa 2 chỗ: `LookupToken` JOIN thêm `operators.disabled_at IS NULL` (token của operator bị disable 401 ngay lập tức, không cần revoke tay — `internal/store/api_tokens.go`); `token create` (CLI) từ chối mint token mới cho operator đã disable (`OperatorEnabled`). Test: `TestIngestDisabledOperatorTokenRejected`.
- **Role gate (Phase 4, `internal/web/role_middleware.go`):** 2 role `admin`/`viewer`. `requireAdmin` middleware **đọc role tươi từ DB mỗi request** (`freshAdminRole`, `SELECT role FROM operators WHERE id=? AND disabled_at IS NULL`) — demote hoặc disable một operator có hiệu lực NGAY, không đợi session cũ hết hạn hoặc phải logout/login lại (test `TestRoleGateDemoteTakesEffectImmediately`, `TestRoleGateDisableTakesEffectImmediately`). Inventory route: **29/38 route POST gate admin**, 9 route còn lại viewer-ok (bảng đầy đủ + test toàn bộ ma trận ở `role_middleware_test.go` → `TestRoleGatesAllPOSTRoutes`); route mới không phân loại mặc định **admin** (fail-closed, `TestRoleGateDefaultDenyOnUnclassifiedRoute`). Local-trust mode (chưa từng có account) bỏ qua role-check hoàn toàn — tương thích v9 single-user.
- **CSRF (C5):** giữ nguyên cơ chế origin-based đã có từ v8 (`originGuard` — Host allowlist + `Origin` header + `Sec-Fetch-Site`), không thêm synchronizer token (không tương thích HTMX `hx-post` hiện tại nếu không viết lại toàn bộ template). Bất biến: không route GET nào có side-effect; E2E cross-origin POST với cookie admin hợp lệ vẫn 403.
- **E2E (Phase 5, `internal/web/identity_rbac_e2e_test.go`):** đặt trong package `internal/web` (theo tiền lệ `v7_writeback_e2e_test.go`) vì chỉ package này thấy cả chi mux lẫn helper test không-export; không dựng `internal/e2e` riêng. 12 test mới dựng `httptest` end-to-end thật (login/session/redirect/CSRF/audit-actor/role/audit-verify/kill-launch-bulk/capture-independence); các bước đã có unit test riêng biệt exhaustive thì **tham chiếu, không nhân bản** (per-operator token spoofed-hint ở `internal/ingest/token_auth_test.go`; role-gate ma trận 38 route + demote/disable-tức-thời ở `role_middleware_test.go`; last-admin-lock + disabled-operator-token ở `auth_middleware_test.go`/`token_auth_test.go`).

Test/build sau v10: `CGO_ENABLED=0 go build ./...`, `go vet ./...`, `go test ./... -count=1` xanh toàn repo.

## v11 (260707, plan `260707-0832`) — Insight expansion

Mở rộng `/insights` với 8 phân tích mới trên `internal/learn` (P1–P5), tận dụng data đã dense hơn từ v9 (hook cài lên 18 project). Descriptive stats thuần — **không** phân tích nào cho vào composite/grade, không rank người (Goodhart).

- **Wilson CI hạ tầng (P1):** `internal/learn/wilson.go` — closed-form Wilson score interval (không cần thư viện thống kê), `FormatWilson(successes, n) string` trả `"— (chưa có mẫu)"` khi n=0, else `"75% (CI 30–95%, n=4)"`. Mọi proportion trong insights (done-rate, ledger outcome, funnel) và — **S7 trọn vẹn (F9)** — leaderboard/metrics dash (`dash_org.html`, `dash_agent.html`) đều hiện CI cạnh con số, không chỉ điểm ước lượng trần. Với Success/Autonomy trên dash, CI dựng từ `Metric.RunIDs` (không đổi struct `learn.Metric`) qua helper template-layer `wilsonFromMetric` (`render.go`) — thuần suy diễn số học từ `Value`+`len(RunIDs)`, không phải trường mới. Acceptance/Reliability không có denominator sạch (RunIDs/EventIDs trộn edit+reject) nên **cố ý không** gắn Wilson — tránh giả vờ chính xác trên một tỉ lệ đã bị trộn mẫu số.
- **Context-Version ROI (P2, `context_roi.go`):** so outcome (done-rate, cost) giữa các *version* context cùng layer/target — trả lời "đổi context có thực sự cải thiện run không". `HasContextData()` gate: DB hiện có 0 hàng `context_injected` trên fleet thật (context hub v5 chưa được cấu hình rộng) → section ship **honest empty-state**, không bịa biểu đồ trống có vẻ như dữ liệu. Steering-numerator tôn trọng 2 capture mode: local (`steering_msg` count) và central (`source='ingest'`, `user_msg` payload cast int) — cùng invariant từ v4.
- **Post-run activity / shadow-work (P3, `shadow_work.go`):** run "done" nhưng work-item liên kết còn bị người sửa **sau khi** run kết thúc → dấu hiệu việc chưa thực sự xong dù agent báo done. `CoverageStat.Insufficient()` gate theo `MinSampleForInsight=3` trên `RunsWithTaskKey` (không phải `RunsTotal`) — coverage thấp hiện rõ "chưa đủ dữ liệu để tin tỉ lệ này", không lặng lẽ hiện một con số mỏng cạnh con số dày.
- **Guardrail ledger (P3, `guardrail_ledger.go`):** đếm **DISTINCT run** bị guardrail chặn (không phải raw event count — 1 run có thể trigger nhiều block cùng rule), gộp theo rule cụ thể khi payload có suffix `(rule #N)` khớp `guardrail_rules`, else gộp theo **class** kiểm tra (G1 regex/G2 sandbox/G3 budget/G4 gate). Mỗi bucket n<3 → badge "chưa đủ mẫu".
- **Time-horizon (P4, `time_horizon.go`):** done-rate theo khoảng thời lượng run (<30m/30m-2h/…). **Zero-contrast (F7):** bucket mà TOÀN BỘ run đều done (không có run fail để so) hiện "chưa có run fail để so" thay vì một tỉ lệ 100% trông như tín hiệu tốt — 100% trên mẫu không có đối chứng không phải là bằng chứng.
- **Spend Pareto (P4, `spend_pareto.go`):** tier chi phí (top-N run chiếm bao nhiêu % tổng $) — run `running` loại khỏi mẫu số (chi phí chưa chốt).
- **Steering economics (P5, `steering_econ.go`):** so outcome giữa run có người phải "chỉnh lái" giữa chừng (`steering_msg`) và không — **taxonomy phân loại nội dung THẬT có tồn tại** (heuristic từ khoá, 5 nhóm cố định + "other"), nhưng hai giới hạn phải nói rõ để không ai hiểu lầm: (1) vị trí trong bảng là **thứ tự tin nhắn** (sequence index), **không phải mốc thời gian đồng hồ** — Dandori không hứa timeline wall-clock cho steering; (2) mẫu hiển thị bị cap (`steeringTextsCap`) nên UI luôn hiện tỉ lệ **kept/total** (`SteeringSummary.KeptTotal`/`CountTotal`) thay vì giả vờ toàn bộ text đã được xét. Taxonomy luôn trả đủ 5 nhóm dù 0 dữ liệu (gate empty-state phải khoá theo `KeptTotal==0`, không theo độ dài slice).
- **Approval funnel (P5, `approval_funnel.go`):** latency qua từng bước duyệt, tính **chỉ trên quyết định người thật** (`status IN ('approved','rejected')`). **F2:** `expired` (TTL sweep tự động) hiện nhãn riêng "TTL sweep, không phải người quyết" — không bao giờ trộn vào mẫu số của "người quyết nhanh hay chậm".
- **Per-section degrade (F13, `handlers_insights.go`):** trước v11, 1 query lỗi → `http.Error` 500 cả trang `/insights`. Với 8 phân tích mới tăng blast radius, đổi sang ô `{Data, Err}` mỗi section — 1 section lỗi chỉ hiện khối "không tải được section này", các section khác vẫn render.
- **E2E gate (F12/F15, `handlers_insights_test.go`):** seed in-mem store với timestamp **cố định** (không `datetime('now')`, window=0 = all-time) cho cả 8 phân tích + 3 phân tích cũ, gọi `GET /insights` thật qua `httptest`, assert số hiện trên HTML == truy vấn SQL thô trên **cùng store**. Test này bắt được 1 bug thật trước khi ship: gate empty-state của steering econ từng khoá nhầm theo độ dài slice-luôn-5-phần-tử thay vì `KeptTotal` (dead code — sửa trong `insights.html`). Gate KHÔNG thay thế live-verify thủ công (xem checklist F12 dưới).

**Honest-data note (bắt buộc đọc trước khi trình bày v11 với ai ngoài team):** so với promise gốc trong `plan.md`, scope thật khi ship là — **taxonomy CÓ thật** (không phải placeholder) nhưng đi kèm 2 caveat trên (seq-index không phải wall-clock, kept/total không phải toàn bộ mẫu); **context-ROI ship với empty-state** vì context hub (v5) chưa được đội nào cấu hình đủ rộng để có version-pair thật để so — section này sẽ tự có dữ liệu khi fleet bắt đầu dùng Context Hub, không cần đổi code.

Manual live-verify checklist (F12, browser-vs-SQL trên data thật): `plans/260707-0832-dandori-v11-insight-expansion/reports/manual-live-verify-checklist-insights-v11.md`.

Test/build sau v11: `CGO_ENABLED=0 go build ./...`, `go vet ./...`, `go test ./... -count=1` xanh toàn repo.

## Giới hạn đã biết / [Sau]

- Tailwind + HTMX + Chart.js qua CDN — cần mạng lần đầu; có CSS fallback tối giản.
- **v6 [Sau]:** launch codex/runtime khác từ console (cần wrap-interception governed trước); remote-machine launch (chỉ localhost máy chạy serve); Windows Job Objects; `EvalSymlinks` hardening cho cwd; SIGTERM giữa run → child detach + reconcile khi restart (không kill child lúc shutdown); RBAC/tác giả per-principal (single-principal `@console`); scheduling/cron, multi-step orchestration.
- **v5 [Sau]:** rollout xấu vẫn tồn tại trong phiên ĐANG chạy đến hết phiên (SessionStart-only, không có đường flush/kill context giữa phiên); RBAC/tác giả per-principal; resolve multi-team hiện lấy team_id nhỏ nhất; re-inject mỗi lần resume; `/ingest/context` đọc được bởi bất kỳ token holder (single shared token, như `/ingest/policy`); import Drive/Confluence, lịch rollout: chưa làm.
- **v4 [Sau]:** RBAC/đa-token per-operator (hiện single shared ingest token + single-principal approval); `rule_candidate_from_repeated_blocks` (observer tự đề xuất regex — cần operator review kỹ); ingest bind ra network thật cần firewall (giả định mạng nội bộ tin cậy); token-level streaming cho chat.
- **v7 [Sau]:** Confluence write cho write-back (chỉ Jira/GWS/GitHub ở v7); RBAC ai được override gate của ai; `post_action_checks` chưa có timeout/kill riêng ngoài context cha; multi-recipient digest chưa nhóm theo team (gửi đồng loạt cho toàn bộ `digest_recipients`).
- **v8 [Sau]:** credential save cần **restart** để worker nhận (không hot-swap config runtime); healthz `last_test` không tự re-probe (token chết ngoài chỉ phát hiện qua `age_days`, không realtime); token lấy từ nguồn ngoài vẫn thủ công (Slack xoxc/xoxd qua DevTools — UI chỉ hạ độ khó phần dán+test); nếu shell env che một key thì sau restart giá trị shell thắng `.env` (đã cảnh báo nhưng chưa tự-migrate); impact estimate advisory-only (không gate gì theo số này).
- **v10 [Sau]:** token TTL chưa có (`expires_at = NULL` vĩnh viễn — opt-in `--expires-in` để sau); non-loopback bind cần HTTPS thật (tunnel/reverse-proxy) để cookie `Secure` có ý nghĩa, tự bind LAN không TLS vẫn là cấu hình không an toàn (chưa có cảnh báo cứng); `saved_views` vẫn global-per-instance, chưa per-operator; RBAC mới có 2 role (admin/viewer) — full granular permission theo route/resource + Google SSO chưa làm; dual-accept ingest token legacy chưa có ngày cutover cụ thể (đề xuất 2 tuần, chưa chốt).
- **v11 [Sau]:** context-ROI trống trên fleet thật cho tới khi đội cấu hình Context Hub rộng hơn (unresolved từ v5); steering taxonomy là heuristic-từ-khoá, chưa có timeline wall-clock (chỉ seq-index); KM/time-horizon survival-style phân tích khi ≥30 run/cohort mới đáng tin (hiện mẫu mỏng nhiều bucket); guardrail ledger per-rule phụ thuộc payload có đúng suffix `(rule #N)` — block không qua rule cụ thể chỉ gộp theo class, không truy được về rule.
- Trend Δ7d là xấp xỉ (window 7d vs 14d), chưa windowing chính xác.
- `internal/web/viewdata.go` 224 dòng (hơi quá chuẩn 200 — thuần query helpers, tách sẽ vụn).
- Cursor/Codex adapter, Confluence/Sheets write, SSO, revert-detection, policy simulator: [Sau].
